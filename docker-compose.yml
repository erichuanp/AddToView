services:
  addtoview:
    build: .
    image: addtoview:latest
    container_name: addtoview
    ports:
      - "2232:2232"
    volumes:
      - ./data:/app/data
    environment:
      - ADDTOVIEW_SERVE_STATIC=1
      - DATA_DIR=/app/data
      - BACKEND_PORT=2232
      # LLM config is configured at runtime via the Settings page; .env
      # values below are an optional dev fallback.
      # - LLM_API_KEY=
      # - LLM_MODEL_ID=
      # - LLM_BASE_URL=
    restart: unless-stopped
