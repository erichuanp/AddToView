# Bilibili One-click Add to "Watch Later"
### Automatically add videos from followed UPs released near a specified date to "Watch Later"
Use a manually prepared cookie json file to read the user's "Watch Later" list and the latest videos published by followed UPs.
In units of pages, add videos within a custom number of days to "Watch Later" and save the list of videos that were not added to "Watch Later".

**Compilation**：Please use pip install to install the required libraries (main dependency: `requests`).

**Usage**：Place AddToView.exe in a separate folder, double-click to run, and if necessary, add a shortcut to the desktop. Prepare `<uid>_cookie.json` in the same directory before running.


# Credits
### Main.py、Core.py
by Me Bilibili：[加把劲假面骑士][1]

[1]: https://space.bilibili.com/4689754
