BACKEND_PORT=8787
DATA_DIR=../data
FRONTEND_ORIGIN=http://localhost:5173

# Optional fallback for the AI summary feature.
# Any OpenAI-compatible chat.completions endpoint works (Doubao, Kimi,
# OpenAI, self-hosted LiteLLM/Ollama, etc.). The user can also configure
# this from the Settings page at runtime — DB values take precedence.
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL_ID=
